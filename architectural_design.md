# تصميم معماري: حل مشاكل الأداء في Memories Recap

## 1. المشكلة الحالية

يواجه مشروع Memories Recap مشاكل إنتاجية كبيرة تتعلق بمعالجة الملفات الكبيرة وتعليق عملية الـ "recap"، مما يحد من قدرته على دعم مئات المستخدمين المتزامنين ورفع ملفات تتجاوز عشرات الميغابايت. تتلخص المشاكل الرئيسية في الآتي:

*   **قيود حجم الملفات**: لا يقبل الموقع ملفات أكبر من عشرات الميغابايت، بينما الهدف هو دعم ملفات تصل إلى 1 جيجابايت أو أكثر.
*   **تعليق عملية الـ Recap**: تبقى عملية معالجة الفيديو معلقة ولا تكتمل، خاصة مع الملفات الأكبر.
*   **قابلية التوسع**: النظام الحالي غير مهيأ للتعامل مع مئات المستخدمين في نفس الوقت.

### التحليل الفني:

من خلال تحليل الكود المصدري والبيئة الحالية، تم تحديد الأسباب الجذرية لهذه المشاكل:

*   **بيئة Vercel Serverless Functions**: تعتمد عملية معالجة الفيديو (التي تتم بواسطة `ffmpeg` في `src/lib/process-job.ts`) على Vercel Serverless Functions. هذه الدوال لها قيود صارمة على وقت التنفيذ (عادة 5 دقائق كحد أقصى)، مما يتسبب في تعليق العمليات الطويلة للملفات الكبيرة. هذا موضح صراحة في `maxDuration = 300` في `src/app/api/cron/process/route.ts` وفي رسالة التحذير `softLimitDurationMessage` في `src/lib/types.ts`.
*   **آلية رفع الملفات**: على الرغم من استخدام `@vercel/blob` و Supabase Storage، فإن طريقة الرفع الحالية قد لا تكون مثالية للملفات الكبيرة جداً، خاصة إذا كانت تمر عبر الـ Serverless Function كـ Proxy قبل الوصول إلى التخزين السحابي. كما أن `app-data` bucket في Supabase له حد 50MB، بينما `memories` و `recaps` لهما حد 5GB، مما يشير إلى أن الملفات الكبيرة يجب أن تذهب مباشرة إلى `memories`.
*   **معالجة الفيديو المكثفة**: استخدام `ffmpeg` لقص ودمج وتعديل الفيديو هي عملية تستهلك الكثير من الموارد (CPU و Memory) وتستغرق وقتاً طويلاً، مما يجعلها غير مناسبة للتشغيل داخل بيئة Serverless Functions ذات الموارد المحدودة.

## 2. الحل المعماري المقترح

للتغلب على هذه التحديات، سيتم إعادة تصميم جزئي لآلية رفع الملفات ومعالجة الفيديو، مع التركيز على فصل المهام الطويلة عن بيئة Vercel Serverless Functions.

### 2.1. نظام رفع الملفات الكبيرة (Direct Upload to Supabase Storage with Presigned URLs)

ستتم إعادة هيكلة عملية رفع الملفات للسماح برفع الملفات الكبيرة مباشرة إلى Supabase Storage، متجاوزةً قيود Vercel Serverless Functions:

1.  **طلب Presigned URL**: عند بدء المستخدم لعملية الرفع، سيقوم الواجهة الأمامية (Client-side) بطلب Presigned URL من الواجهة الخلفية (Backend API). هذا الـ URL سيسمح للعميل بالرفع مباشرة إلى Supabase Storage دون الحاجة إلى المرور عبر خادم Vercel.
2.  **الرفع المباشر**: سيقوم العميل برفع الملفات (حتى 1 جيجابايت أو أكثر) مباشرة إلى Supabase Storage باستخدام الـ Presigned URL. هذا يزيل قيود حجم الطلب في Vercel ويحسن سرعة الرفع.
3.  **إشعار الاكتمال**: بعد اكتمال الرفع إلى Supabase Storage، سيقوم العميل بإرسال إشعار إلى الواجهة الخلفية لتأكيد اكتمال الرفع وتحديث حالة المهمة في قاعدة البيانات.

**التعديلات المطلوبة:**

*   **الواجهة الخلفية (`src/app/api/uploads/route.ts` أو ما شابه)**: إضافة نقطة نهاية (API Endpoint) لإنشاء Presigned URLs لـ Supabase Storage. ستحتاج هذه النقطة إلى التحقق من صلاحية المستخدم وحجم الملف المطلوب.
*   **الواجهة الأمامية (`src/components/upload-workspace.tsx`)**: تعديل منطق الرفع لاستخدام الـ Presigned URLs بدلاً من `@vercel/blob/client` أو الرفع المباشر عبر Vercel API. سيشمل ذلك التعامل مع Progress Bar وإدارة الأخطاء.
*   **Supabase Storage**: التأكد من أن سياسات (Policies) Supabase Storage تسمح بإنشاء Presigned URLs والرفع المباشر مع الحفاظ على الأمان.

### 2.2. معالجة الـ Recap في الخلفية (Background Processing with a Dedicated Worker)

لحل مشكلة تعليق عملية الـ recap وتجاوز قيود وقت التنفيذ في Vercel Serverless Functions، سيتم نقل عملية معالجة الفيديو إلى خدمة عامل (Worker Service) مخصصة:

1.  **فصل مهمة المعالجة**: سيتم فصل دالة `processJob` (الموجودة حالياً في `src/lib/process-job.ts`) عن سياق Vercel Serverless Function.
2.  **نظام قائمة انتظار المهام (Job Queue)**: عند اكتمال رفع الملفات، بدلاً من استدعاء `processJob` مباشرة، سيتم إضافة مهمة جديدة إلى قائمة انتظار مهام (Job Queue). يمكن استخدام حلول مثل Inngest (إذا كان متوفراً ومناسباً) أو بناء نظام قائمة انتظار بسيط باستخدام Supabase Database (مثل جدول `jobs` الحالي مع تحديثات الحالة) وخدمة عامل خارجية.
3.  **خدمة عامل مخصصة (Dedicated Worker Service)**: سيتم نشر خدمة عامل منفصلة (يمكن أن تكون على منصة مثل Render.com، Fly.io، أو حتى Virtual Machine) مهمتها الوحيدة هي مراقبة قائمة انتظار المهام ومعالجة المهام المعلقة. هذه الخدمة لن تكون مقيدة بحدود وقت التنفيذ الخاصة بـ Vercel Serverless Functions.
4.  **تحديث الحالة**: ستقوم خدمة العامل بتحديث حالة المهمة (Progress, Status) في Supabase Database بشكل دوري، مما يسمح للواجهة الأمامية بعرض التقدم للمستخدم.
5.  **إدارة الأخطاء وإعادة المحاولة**: يجب أن يتضمن نظام العامل آليات قوية لإدارة الأخطاء وإعادة محاولة المهام الفاشلة.

**التعديلات المطلوبة:**

*   **إزالة `processJob` من Vercel Cron**: سيتم إزالة استدعاء `processJob` من `src/app/api/cron/process/route.ts`.
*   **نظام قائمة انتظار المهام**: تعديل الواجهة الخلفية لإضافة المهام إلى قائمة انتظار بدلاً من المعالجة الفورية.
*   **خدمة عامل جديدة**: تطوير ونشر خدمة عامل منفصلة بلغة Node.js (باستخدام `ffmpeg-static` و `fluent-ffmpeg` كما هو الحال حالياً) لمراقبة قائمة انتظار المهام ومعالجة الفيديو.
*   **تحديثات قاعدة البيانات**: التأكد من أن خدمة العامل تقوم بتحديث جدول `jobs` في Supabase بشكل صحيح ليعكس حالة التقدم والأخطاء.

## 3. تحسين الأداء وقابلية التوسع

بالإضافة إلى الحلول المعمارية المذكورة أعلاه، سيتم النظر في التحسينات التالية لضمان الأداء وقابلية التوسع:

*   **تحسين استخدام FFmpeg**: مراجعة معلمات `ffmpeg` في `src/lib/process-job.ts` لتحسين الأداء دون المساس بالجودة، مثل استخدام أكواد (Codecs) أكثر كفاءة أو تحسين تسلسل العمليات.
*   **توزيع المهام**: إذا كان عدد المستخدمين كبيراً جداً، يمكن توزيع المهام على عدة خدمات عاملة (Workers) لزيادة الإنتاجية.
*   **مراقبة الأداء**: إعداد أدوات مراقبة (Monitoring) لخدمة العامل وقاعدة البيانات لتحديد أي اختناقات في الأداء بشكل استباقي.
*   **تحسين استعلامات Supabase**: مراجعة استعلامات قاعدة البيانات في `src/lib/store.ts` و `src/lib/jobs.ts` للتأكد من كفاءتها، خاصة مع زيادة حجم البيانات.

## 4. الخطوات التالية

1.  **تعديل الواجهة الأمامية والخلفية لرفع الملفات**: تنفيذ نظام Presigned URLs للرفع المباشر إلى Supabase Storage.
2.  **إنشاء خدمة عامل مخصصة**: تطوير ونشر خدمة عامل منفصلة لمعالجة الفيديو.
3.  **تعديل منطق Cron Job**: تحويل `src/app/api/cron/process/route.ts` لإضافة المهام إلى قائمة الانتظار بدلاً من معالجتها مباشرة.
4.  **الاختبار الشامل**: اختبار النظام الجديد بملفات كبيرة وعدد كبير من المستخدمين المتزامنين للتأكد من استقراره وأدائه.

هذا التصميم سيمكن Memories Recap من التعامل مع متطلبات الأداء العالية ودعم تجربة مستخدم سلسة حتى مع الملفات الكبيرة جداً.
