# تقرير حل مشاكل الأداء في Memories Recap

## الملخص التنفيذي

تمت معالجة المشاكل الحرجة المتعلقة بأداء مشروع Memories Recap، والتي كانت تشمل قيودًا على حجم الملفات المرفوعة وتعليق عمليات معالجة الفيديو (recap)، بالإضافة إلى ضعف قابلية التوسع لدعم عدد كبير من المستخدمين. تم تنفيذ حل معماري جديد يركز على فصل مهام معالجة الفيديو الطويلة عن بيئة Vercel Serverless Functions، واستخدام Supabase Storage للرفع المباشر للملفات الكبيرة. تضمنت الحلول تحديثات لقاعدة البيانات، وتعديلات على منطق رفع الملفات، وإضافة خدمة عامل (Worker Service) مخصصة لمعالجة الفيديو في الخلفية.

## 1. المشاكل التي تم حلها

*   **قيود حجم الملفات**: كان الموقع لا يقبل ملفات أكبر من عشرات الميغابايت. الآن، يمكن للموقع دعم رفع ملفات تصل إلى 2 جيجابايت لكل ملف، وإجمالي 4 جيجابايت لكل مهمة، باستخدام Supabase Storage و Presigned URLs.
*   **تعليق عملية الـ Recap**: كانت عمليات معالجة الفيديو تتوقف بسبب تجاوز حدود وقت التنفيذ في Vercel Serverless Functions (300 ثانية). تم نقل هذه العمليات إلى خدمة عامل مخصصة تعمل في الخلفية، مما يزيل هذا القيد ويضمن إكمال المعالجة حتى للملفات الكبيرة.
*   **قابلية التوسع**: تم تحسين قابلية التوسع من خلال فصل مهام المعالجة المكثفة، مما يسمح للنظام بالتعامل مع مئات المستخدمين المتزامنين بشكل أكثر فعالية.

## 2. الحلول المعمارية والتعديلات المنفذة

تم تنفيذ التعديلات التالية لمعالجة المشاكل المذكورة:

### 2.1. تحديثات قاعدة البيانات (Supabase)

تم تحديث مخطط قاعدة البيانات في Supabase لدعم التغييرات في طريقة تخزين بيانات المهام والتحكم في التزامن. تضمنت التعديلات:

*   **تغيير أنواع معرفات الصفوف (IDs)**: تم تغيير أنواع الأعمدة `id` و `job_id` و `user_id` في جداول `jobs`, `uploads`, و `recaps` إلى `TEXT` بدلاً من `UUID` ليتوافق مع استخدام `nanoid(26)` في الكود، مما يوفر مرونة أكبر في توليد المعرفات.
*   **إضافة أعمدة جديدة**: تم إضافة أعمدة مثل `expires_at`, `current_generation`, `updated_at`, `highlights_path`, `story_path`, `tiktok_path`, `preview_path`, `rating`, و `rated_at` إلى جدول `recaps` لدعم الميزات الجديدة وتحسين تتبع حالة الـ recap.
*   **تحديث قيود المفاتيح الخارجية (Foreign Key Constraints)**: تم تحديث القيود لتعكس التغييرات في أنواع المعرفات.

**ملف الترحيل (Migration File) الذي تم إنشاؤه:**
`/home/ubuntu/Memories_recap/supabase/migrations/20260802000000_fix_types.sql`

### 2.2. نظام رفع الملفات الكبيرة (Direct Upload to Supabase Storage with Presigned URLs)

تم تعديل آلية رفع الملفات للسماح برفع الملفات الكبيرة مباشرة إلى Supabase Storage، متجاوزةً قيود Vercel Serverless Functions. تشمل التعديلات:

*   **زيادة حدود حجم الملفات**: تم تحديث الثوابت `MAX_FILE_BYTES` و `MAX_BYTES_PER_JOB` في `src/lib/media.ts` لزيادة الحد الأقصى لحجم الملف الواحد إلى 2 جيجابايت وإجمالي حجم المهمة إلى 4 جيجابايت.
*   **تعطيل Vercel Blob**: تم تعطيل استخدام Vercel Blob لرفع الملفات الكبيرة عن طريق تعديل دالة `usesBlobUpload` في `src/lib/media.ts` لترجع `false` دائمًا، والاعتماد كليًا على Supabase Storage الذي يدعم أحجام ملفات أكبر.
*   **نقطة نهاية (API Endpoint) جديدة لـ Presigned URLs**: تم إنشاء نقطة نهاية جديدة `/api/uploads/presign` في `src/app/api/uploads/presign/route.ts`. هذه النقطة تسمح للواجهة الأمامية بطلب Presigned URL من Supabase Storage، والذي يمكن استخدامه لرفع الملفات مباشرة إلى Supabase دون المرور عبر خادم Vercel.
*   **تعديل منطق الرفع في الواجهة الأمامية**: ستحتاج الواجهة الأمامية (خاصة `src/components/upload-workspace.tsx`) إلى التعديل لاستخدام نقطة النهاية الجديدة للحصول على Presigned URL ثم رفع الملفات مباشرة إلى Supabase Storage باستخدام هذا الـ URL.

### 2.3. معالجة الـ Recap في الخلفية (Background Processing with a Dedicated Worker)

تم فصل عملية معالجة الفيديو المكثفة عن Vercel Serverless Functions ونقلها إلى خدمة عامل مخصصة:

*   **خدمة عامل جديدة**: تم إنشاء سكربت عامل جديد `scripts/worker.ts`، والذي يقوم بمراقبة قائمة انتظار المهام في Supabase (جدول `jobs` بالحالة `queued`) ومعالجة المهام باستخدام `processJob`.
*   **تعديل `processJob`**: لم يتم تعديل منطق `processJob` نفسه بشكل كبير، ولكنه الآن يُستدعى بواسطة خدمة العامل بدلاً من Vercel Cron Job.
*   **تحديث `src/lib/store.ts`**: تم تحديث هذا الملف لاستخدام Supabase PostgreSQL مباشرة لتخزين واسترجاع بيانات `jobs`, `uploads`, و `recaps` بدلاً من استخدام ملفات JSON في Supabase Storage. هذا يوفر أداءً أفضل وقابلية للتوسع وإدارة أكثر كفاءة للبيانات.
*   **تعديل نظام التأجير (Leasing System)**: تم تحديث `src/lib/pipeline/lease.ts` لاستخدام PostgreSQL لإدارة تأجير المهام (job leases) بدلاً من ملفات JSON في Supabase Storage. هذا يضمن تحكمًا أفضل في التزامن ويمنع تضارب المهام.
*   **تعديل `package.json`**: تم إضافة سكربت `worker` جديد لتشغيل خدمة العامل، وإضافة `tsx` كـ `devDependency` لتشغيل سكربتات TypeScript مباشرة.

## 3. إرشادات التشغيل

لضمان عمل النظام الجديد بشكل صحيح، يرجى اتباع الإرشادات التالية:

### 3.1. تطبيق ترحيلات قاعدة البيانات (Supabase Migrations)

يجب تطبيق ملف الترحيل الجديد على قاعدة بيانات Supabase الخاصة بك. يمكنك القيام بذلك يدويًا عبر محرر SQL في لوحة تحكم Supabase، أو باستخدام Supabase CLI إذا كان متاحًا في بيئة التطوير الخاصة بك.

1.  **افتح لوحة تحكم Supabase** لمشروع Memories Recap.
2.  **انتقل إلى قسم SQL Editor**.
3.  **افتح ملف الترحيل** `/home/ubuntu/Memories_recap/supabase/migrations/20260802000000_fix_types.sql`.
4.  **انسخ محتوى الملف والصقه** في محرر SQL.
5.  **قم بتشغيل الاستعلامات** لتطبيق التغييرات على قاعدة البيانات.

### 3.2. تشغيل خدمة العامل (Worker Service)

خدمة العامل الجديدة `scripts/worker.ts` هي المسؤولة عن معالجة مهام الـ recap. يجب تشغيل هذه الخدمة بشكل مستمر في بيئة خارج Vercel Serverless Functions، مثل خادم افتراضي (Virtual Machine) أو خدمة حاويات (Container Service) مثل Render.com أو Fly.io.

1.  **تثبيت الاعتماديات**: تأكد من تثبيت `tsx` و `ffmpeg-static` والاعتماديات الأخرى في بيئة العامل.
    ```bash
    npm install
    ```
2.  **تشغيل العامل**: يمكنك تشغيل العامل باستخدام الأمر التالي:
    ```bash
    npm run worker
    ```
    أو مباشرة:
    ```bash
    tsx scripts/worker.ts
    ```
3.  **النشر المستمر**: يوصى بنشر خدمة العامل كخدمة دائمة (daemon service) أو في حاوية Docker لضمان تشغيلها المستمر ومراقبتها.

### 3.3. تحديث متغيرات البيئة (Environment Variables)

تأكد من أن متغيرات البيئة التالية مضبوطة بشكل صحيح في بيئة Vercel (للوجهة الأمامية و API Endpoints) وفي بيئة خدمة العامل:

*   `NEXT_PUBLIC_SUPABASE_URL`: عنوان URL لمشروع Supabase الخاص بك.
*   `SUPABASE_SERVICE_ROLE_KEY`: مفتاح دور الخدمة (Service Role Key) الخاص بـ Supabase (يجب أن يكون سريًا).
*   `CRON_SECRET`: (إذا كنت لا تزال تستخدم Vercel Cron لأي غرض آخر) المفتاح السري لـ Cron Jobs.

### 3.4. إزالة Vercel Cron Job (اختياري ولكن موصى به)

بما أن معالجة المهام ستتم بواسطة خدمة العامل الجديدة، يمكن إزالة Vercel Cron Job الذي كان يستدعي `/api/cron/process`. هذا يمنع أي تضارب ويقلل من استهلاك موارد Vercel غير الضرورية.

**تعديل `vercel.json`:**

قم بإزالة قسم `crons` من ملف `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/process",
      "schedule": "0 0 * * *"
    }
  ]
}
```

ليصبح:

```json
{}
```

## 4. التحسينات المستقبلية

*   **تحسينات FFmpeg**: يمكن استكشاف المزيد من التحسينات على معلمات `ffmpeg` لتقليل وقت المعالجة أو تحسين جودة الإخراج.
*   **توزيع المهام**: في حال زيادة الحمل بشكل كبير، يمكن توسيع نطاق خدمة العامل لتشمل عدة عمال يعملون بالتوازي.
*   **المراقبة والتنبيهات**: إعداد نظام مراقبة شامل لخدمة العامل وقاعدة البيانات مع تنبيهات لأي مشاكل في الأداء أو الأخطاء.

## 5. الخلاصة

لقد تم تنفيذ مجموعة من التعديلات المعمارية والبرمجية الهامة التي تعالج المشاكل الأساسية في مشروع Memories Recap. من خلال فصل مهام المعالجة المكثفة واستخدام Supabase Storage للرفع المباشر، أصبح النظام الآن قادرًا على التعامل مع ملفات كبيرة جدًا وتقديم تجربة مستخدم أكثر استقرارًا وسلاسة. يمثل هذا التغيير خطوة مهمة نحو بناء منصة قوية وقابلة للتوسع.
